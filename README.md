# Blog-Posts-Manager-
A Flutter CRUD application that manages blog posts using the JSONPlaceholder API. Built with Provider for state management and the HTTP package for API consumption. Features include creating, reading, updating, and deleting posts with clean architecture, loading states, and error handling.
